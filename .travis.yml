language: python
python:
  - "3.5"
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest --cov mamba --doctest-modules --pep8 coveralls --cov-coveralls --cov-report term-missing
after_success:
  - coveralls
